specVersion: "v1beta"
serviceId: "goalmatic"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "goalmatic-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
